# v0.1.0

First tagged cut of the site. `main` promoted onto `release`.

Tag: `v0.1.0`  
SHA: `8a8a09e` (`fix: serve photos from public instead of a symlink`)

## Apps

- Photos journal. EXIF/IPTC/XMP for date and place. Files live in `public/photos` so Vercel can serve them (the old `src/images/photos` symlink 404'd).
- Bookmarks: named collections, OG snapshots, `bookmarks:sync` / `bookmarks:fetch`.
- Inspirations pinboard.
- Music player.
- Package analyzer for npm trees.
- Apps gated with Vercel Flags (`feature-app-*`). Off in production until the flag is on.

## Site

- Connect page.
- Posts on fumadocs. Code copy, package-manager tabs, shiki in the terminal component.
- View Transitions between routes.
- Colophon on tool pages.
- Landing and visual language in `.impeccable.md`.

## Platform

- Next.js 16, React 19, Node 24, pnpm 10.31.
- Replaces the Next 14.2.35 CVE pin that lived only on `release`.

## After merge

Tag `v0.1.0` is on `8a8a09e`. GitHub release is drafted. After this PR lands on `release`, publish it:

```bash
gh release edit v0.1.0 --draft=false
```
