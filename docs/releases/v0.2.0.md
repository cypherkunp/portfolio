# v0.2.0

Second tagged cut. `main` promoted onto `release`.

Tag: `v0.2.0`  
SHA: `5cd3cfb` (`feat: enhance email link handling in ConnectBlock and ExternalLink components`)

## Site

- Canonical host is `devvrat.cc`. Favicon, Apple touch icon, Android chrome icon, web manifest, and a shared SEO helper for home, about, and posts.
- About page copy and FAQ rewritten.
- Connect `mailto:` stays in the same tab. Other external links still open in a new tab.

## Apps

- Bookmark catalog shows the snapshot image, and hides the thumb column when every bookmark in the collection has `image: null`.
- Collections and bookmarks share one snapshot store.
- Homepage app links, feature flags, and the sitemap read one catalog, so Bookmarks and Inspirations are in the sitemap.

## Posts

- "GitHub stacked pull requests."

## Platform

- Vitest and React Testing Library.
- Playwright e2e and a pre-push hook.
- Turbo skips unchanged `test` and `test:e2e` on a cache hit.

## After merge

Tag `v0.2.0` is on `5cd3cfb`. GitHub release is drafted. After this PR lands on `release`, publish it:

```bash
gh release edit v0.2.0 --draft=false
```
